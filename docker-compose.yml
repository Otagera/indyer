services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: indyer
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-indyer}
      POSTGRES_DB: indyer
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - indyer

  api:
    image: ghcr.io/otagera/indyer-api:latest
    pull_policy: always
    depends_on:
      - postgres
    environment:
      DATABASE_URL: postgres://indyer:${POSTGRES_PASSWORD}@postgres:5432/indyer
      NODE_ENV: production
      PORT: 3001
      CORS_ORIGIN: https://indyer.otagera.xyz
    ports:
      - "3001:3001"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.indyer-api.rule=Host(`indyer-api.otagera.xyz`)"
      - "traefik.http.routers.indyer-api.entrypoints=https"
      - "traefik.http.routers.indyer-api.tls=true"
      - "traefik.http.routers.indyer-api.tls.certresolver=letsencrypt"
      - "traefik.http.services.indyer-api.loadbalancer.server.port=3001"
    networks:
      - indyer

  client:
    image: ghcr.io/otagera/indyer-client:latest
    pull_policy: always
    depends_on:
      - api
    environment:
      - VITE_API_URL=https://indyer-api.otagera.xyz
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.indyer-client.rule=Host(`indyer.otagera.xyz`)"
      - "traefik.http.routers.indyer-client.entrypoints=https"
      - "traefik.http.routers.indyer-client.tls=true"
      - "traefik.http.routers.indyer-client.tls.certresolver=letsencrypt"
      - "traefik.http.services.indyer-client.loadbalancer.server.port=5173"
    networks:
      - indyer

volumes:
  pgdata:

networks:
  indyer:
